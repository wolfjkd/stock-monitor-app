# Changelog

## v4.5 (2026-06-11)

### 新增功能
- **大单标记模块**：实时检测监控股票的异常大单成交
  - 数据源：eltdx通达信协议逐笔成交数据
  - 检测阈值：单笔≥12000手
  - 轮询间隔：3秒
  - 多列布局：按监控股票数量自动分列，列头显示股票名称
  - 颜色标识：买盘红色、卖盘绿色
  - SSE实时推送到前端
  - 每只股票最多保留50条记录

### UI交互优化
- 卡片股票名：字号17px加粗，价格20px，涨跌幅12px
- 监控配置表格：字号14px，目标价列16px加大加粗，行列居中显示
- 大单标记/预警日志面板：上下布局，大单标记占3/5，预警日志占2/5，可拖拽调整
- 预警日志字体：14px
- 列宽独立调节：table-layout:auto + 同步tbody列宽
- 删除监控配置表冗余列：代码、涨跌幅、当前价、状态
- 卡片区域去掉目标价显示

### Bug修复
- 修复卡片股票名/代码来回闪烁的bug（优先取配置name）

## v4.3 (2026-06-10)

### 新增功能
- **CSV一键导入**：监控配置面板新增"一键导入"按钮，支持导入券商导出的CSV文件
  - 支持GBK编码（国内券商/Excel默认格式）
  - 自动识别字段：证券代码/委托类型/委托价格/委托数量
  - 字段映射：SH.603077→sh603077，融资买入→多仓+跌破提醒，现金卖出→空仓+涨破提醒
  - 委托数量自动转换为手数（股数÷100）
  - 追加模式，不覆盖现有配置
- **批量操作**：表格新增勾选列，支持全选/多选/批量删除
  - 表头全选勾选框（支持半选状态indeterminate）
  - 每行独立勾选框
  - 勾选后底部显示操作栏：已选数量 + 批量删除 + 取消选择
  - 批量删除含确认弹窗
- **拖拽排序**：每行左侧添加拖拽手柄，支持鼠标拖动调整顺序
  - 同一只股票整体移动，不会拆散
  - 拖拽中虚线提示目标位置
- **股票名称自动识别**：CSV导入或手动添加后，异步调用腾讯接口批量获取股票中文名
- **表格自动排序**：按股票名称分组，同股按目标价降序（高价在上），同价空仓在上

### UI交互优化
- 目标价列突出显示：两侧3px蓝色边框、字号加大(13px)加粗、淡蓝底色
- 目标价数字颜色跟随委托方向：多仓红色、空仓绿色
- 手数列颜色同步跟随委托方向
- 股票分组分割线加粗加色（2px→3px蓝色#4a90d9）
- 分组背景色加深，区分更明显
- 表格列宽可拖拽调整
- 监控配置与预警日志面板比例可拖拽调整
- 首次触发预警：右上角弹窗 + 三声提示音（不抢焦点，8秒自动消失）

### Bug修复
- 修复后端 change_pct 与前端 changePct 字段名不匹配导致涨跌幅不显示
- 修复CSV导入GBK编码导致表头乱码匹配失败
- 修复勾选状态被行情刷新（5秒周期）覆盖丢失的bug
- 修复默认配置包含实际监控数据的问题（改为示例数据）

### 排序规则
- 一级：按股票名称拼音分组（localeCompare zh）
- 二级：同股按目标价降序（高价在上）
- 三级：同价按委托方向（空仓在上）

## v4.1 (2026-06-09)

### 功能增强
- 监控配置新增字段：手数、委托方向（多仓/空仓）、状态（启用/暂停）
- 暂停状态的股票：表格半透明显示，不触发预警，但仍获取行情
- 移除同一股票只能添加一条的限制（支持同股多目标价）

### UI交互优化
- 表格列宽可拖拽调整（鼠标拖动列头边缘）
- 监控配置与预警日志面板比例可拖拽调整
- 首次触发预警：右上角弹窗 + 三声提示音（不抢焦点，8秒自动消失）
- 目标价/手数列：多仓红色加粗，空仓绿色加粗

### Bug修复
- 修复后端 change_pct 与前端 changePct 字段名不匹配导致涨跌幅不显示的bug

## v4.0 (2026-06-09)

### 首次发布
- 独立Windows桌面应用，基于Flask+pywebview+pystray架构
- 系统托盘后台运行，关闭窗口不退出
- Edge WebView2渲染引擎
- 实时行情展示（TDX通达信协议直连）
- 多TDX节点支持，一键切换
- 预警日志（SSE实时推送）
- 配置管理（增删改查监控股票）
- PyInstaller打包，生成独立exe（约6.6MB）
